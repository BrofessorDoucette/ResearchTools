def month_str_from_int(month : int):
    '''Just adds a zero before the month if its less than 10.'''
    return f"{month:02d}"


def day_str_from_int(day : int):
    '''Just adds a zero before the day if its less than 10.'''

    return f"{day:02d}"